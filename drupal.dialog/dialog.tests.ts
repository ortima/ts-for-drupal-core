/// <reference path="./index.d.ts" />

((Drupal, drupalSettings) => {
  const {
    autoOpen,
    autoResize,
    buttonClass,
    buttonPrimaryClass,
    close,
    dialogClass,
    maxHeight,
    modal,
  } = drupalSettings.dialog
})(Drupal, drupalSettings)
